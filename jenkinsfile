pipeline {
    agent {
        // Use a node with Node.js and npm installed
        label 'nodejs'
    }

    stages {
        stage('Checkout') {
            steps {
       git 'https://github.com/R807989/main-hrm.git'         
               
            }
        }
      }
