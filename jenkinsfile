pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                
            }
        }

        stage('Build') {
            steps {
                sh 'npm install'
                sh 'npm run build'
            }
        }

        }
}
}
}
