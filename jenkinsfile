pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/R807989/main-hrm.git'
            

        stage('Build') {
            steps {
                sh 'npm install'
                sh 'npm run build'
}
}
            }
        }
    }
}
